const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
test('clean V3 booking returns a token-protected payment status handoff',()=>{const intent=fs.readFileSync(path.join(__dirname,'..','api','v3','booking-intents.js'),'utf8'),status=fs.readFileSync(path.join(__dirname,'..','api','v3','booking-status.js'),'utf8');assert.match(intent,/payload\.status_token/);assert.match(intent,/\/api\/v3\/booking-status\?booking_id=/);assert.match(status,/\/api\/v3\/public\/booking-status\?booking_id=/);assert.match(status,/payment_url/);assert.match(status,/startsWith\('https:\/\/'\)/);assert.doesNotMatch(intent,/create-checkout-session|STRIPE_SECRET_KEY|\/api\/v2/);assert.doesNotMatch(status,/SUPABASE_SERVICE_ROLE_KEY|\/api\/v2/)});