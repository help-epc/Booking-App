{
  "name": "epc-pro-booking-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm test && node build.mjs",
    "check": "node scripts/check.js",
    "test": "node --test tests/v3-*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.1",
    "stripe": "18.5.0"
  }
}
